#!/usr/bin/env bash

echo 'Pretend building '$TOOL_NAME

cd repo
# autoconf && ./configure --prefix $PKT_OUTPUT_DIR
# ln /usr/include/FlexLexer.h /mingw64/include  # Verilator only
# make
# make test
# make install
cd ..
